using System;

namespace myForecast
{
    public enum WeatherUnit
    {
        Metric,
        Imperial
    }

    public enum ClockTimeFormat
    {
        Hours12,
        Hours24
    }

    public enum Language
    {
        en,
        fr
    }
}
